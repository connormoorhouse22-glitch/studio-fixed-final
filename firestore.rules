rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all authenticated users full access to verify connection
    match /{document=**} {
      allow read, write: if true;
    }
  }
}